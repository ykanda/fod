# Issue 16 調査レポート

## 症状
フィルター文字列入力時に日本語を入力し、バックスペースで削除すると文字化けする。

## 原因
`FilenameFilter.delCharacter()` が文字列をバイト長で 1 バイト削除しており、UTF-8 のマルチバイト文字を途中で切断していたため。

該当箇所: `pkg/filter_filename.go` の `delCharacter()`

## 修正方針
バイト単位ではなく rune 単位で末尾 1 文字を削除する。

## 影響範囲
フィルタ入力のバックスペース処理のみ。既存の ASCII 文字入力には影響なし。
